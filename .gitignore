# Firebase 서비스 계정 키 - 절대 커밋 금지
serviceAccountKey.json

# 의존성
node_modules/

# 환경 변수
.env
.env.*
.dev.vars

# 빌드 산출물 (vite build 결과 — 배포 시 Cloudflare가 직접 빌드한다)
dist/

# wrangler 로컬 캐시
.wrangler/

# 로그
*.log
